export interface MenuItem {
    id: number;
    id_number: string;
    name: string;
    price: string;
    category: string;
}

export interface BuffetItem {
    id: number;
    name: string;
    price: string;
    category: string;
}